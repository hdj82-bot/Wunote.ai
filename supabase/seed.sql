-- Wunote seed data — minimal defaults shared by every fresh Supabase instance.
-- NOTE: real auth.users are created via the signup flow; this file only seeds
-- data that is safe to exist before any user registers.

-- Placeholder for HSK base corpus (actual content loaded separately via admin script).
-- Kept empty so that `supabase db reset` completes cleanly.
